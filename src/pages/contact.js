import React from 'react';
import Layout from '../components/Layout/Layout';

export default () => {
    return (
        <Layout>
            <main>
                <h1>Contact Page</h1>
            </main>
        </Layout>
    );
};
